# ReckLock Discover runs entirely on your machine. No cloud APIs & no telemetry.
# These variables are optional hints for local tooling only — the CLI does not require a .env file.

# Example: default output directory for CI wrappers (not read by the scanner CLI today)
# RECKLOCK_SCANNER_OUTPUT_DIR=reports
